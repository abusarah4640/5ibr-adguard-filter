# Install as /etc/fivebr-web.env with mode 600 and root ownership.
# Never commit the real secret.
FIVEBR_ENV=production
FIVEBR_HOME=/opt/5ibr
FIVEBR_SECRET_KEY=<generate-a-random-secret>
FIVEBR_TRUSTED_HOSTS=filters.example.com
FIVEBR_COOKIE_SECURE=true
